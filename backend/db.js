var users = require('./db.json');
var cardapio = require('./cardapio.json');
  return {
    users: users,
    cardapio: cardapio,
    produto: top,
    pedidos: pedidos,
    carrinho: carrinho,
    fornecedor: fornecedor,
  };
};